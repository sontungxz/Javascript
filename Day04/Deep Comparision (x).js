/**đầu tiên sánh ===  băng nhau cả về giá trị và kiểu hay không
 * so sánh số lượng key của object
 *  so sanh prop trong object
 *
 *
 * */
function(obj1, obj2){
    if(Object.keys(obj1).length !== Object.keys(obj2).length)
    return false;
}